spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password: ""
  flyway:
    url: jdbc:h2:mem:testdb
    user: sa
    password: ""
    locations: classpath:db/migration
  h2:
    console:
      enabled: true
      path: /h2-console
logging:
  level:
    root: INFO
